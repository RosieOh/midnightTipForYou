# ========================================
# 메인 설정 파일 (Main Configuration)
# ========================================

# 기본 프로파일 설정 (기본값: dev)
spring.profiles.active=dev

# 공통 설정 파일 포함
spring.config.import=classpath:application-common.properties

# 환경별 설정 파일 자동 로드
# - application-dev.properties (개발 환경)
# - application-prod.properties (운영 환경)
